package ui

import (
	"fl/helpers"
	"fl/io"
	"fmt"
)

func RunProgram(Flags *helpers.FlagStruct, Config *io.Config) error {
	fmt.Println("TUI is disabled for now - see '-h' for CLI usage!")
	return nil
}
